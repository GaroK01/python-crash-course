Title: Every Function

Exercise: Think of something you could store in a list. For example,
you could make a list of mountains, rivers, countries, cities, languages, or any-
thing else you’d like. Write a program that creates a list containing these items
and then uses each function introduced in this chapter at least once.

Answer:
languages = ['Armenian']
number_of_languages = len(languages)

print(f'I know {number_of_languages} language only.')
print(languages)

languages.append('French')
languages.insert(1, 'English')
languages.append('Greek')
languages.append('Arabic')
print(languages)

del (languages[3])
languages.pop()
print(languages)

languages.append('Arabic')
languages.append('Spanish')

number_of_languages = len(languages)
print(f'I know now {number_of_languages} languages!')
languages.reverse()
print(languages)
languages.sort()
print(languages)
print(sorted(languages, reverse=True))
