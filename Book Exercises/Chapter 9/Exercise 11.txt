Title: Imported Admin

Exercise: Start with your work from Exercise 9-8 (page 178).
Store the classes User, Privileges, and Admin in one module. Create a sepa-
rate file, make an Admin instance, and call show_privileges() to show that
everything is working correctly.

Asnwer:
# The files will be included in the chapter 9 folder

from user import Admin

garo = Admin('Garo', 'Kalfayan', 'GaroK01', 'garokalfayan31@gmail.com', 'Lebanon')
garo.describe_user()

garo.privileges = ['Password reset', 'Create new accounts', 'Account suspension']
garo.privileges.privileges = garo_privileges
garo.privileges.show_privileges()
